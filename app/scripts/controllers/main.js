'use strict';

/**
 * @ngdoc function
 * @name dawnApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the dawnApp
 */
angular.module('dawnApp')
    .controller('MainCtrl', function($scope) {
        $scope.awesomeThings = [
            'HTML5 Boilerplate',
            'AngularJS',
            'Karma'
        ];

        
        
    });
